services:
  postgres:
    image: 'postgres:16-alpine'
    restart: unless-stopped
    environment:
      - 'POSTGRES_DB=agenda_db'
      - 'POSTGRES_PASSWORD=secret_1234'
      - 'POSTGRES_USER=besysoft'
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/data/postgres
volumes:
  pgdata: